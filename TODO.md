# TODO

## Now

- [ ] Create remote repos (Gitea/GitHub) for WEB-Startr.Team and startr-team-py; push
- [ ] Browser-review every spec page against the library's actual interfaces
- [ ] Point startr.team DNS at the deployed container

## Next

- [ ] Migrate Lia Fáil onto `startr-team` (delete its vendored engine) — committed fast follow-up
- [ ] Migrate Downes after Lia Fáil
- [ ] Publish `nlt-tools` to PyPI, then `startr-team` (unblocks plain `pip install`)
- [ ] Resolve nlt-tools (AGPL) ↔ startr-team (Apache-2.0) licensing interaction

## Later

- [ ] Spec + implement intake (`Inbox` seam — IMAP first), per the messaging page's sketch
- [ ] Graph plans v0.2: named state machines (`type: state`, guards, events)
- [ ] Syntax highlighting on spec code blocks (build-time, no CDN JS)
- [ ] Social share images for spec pages

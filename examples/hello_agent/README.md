The smallest Startr.Team agent: core only, no messaging. One persona file, one
registered demo tool, one call to `Agent().run(...)`. Every run writes a
`vault/` directory you can read: the plan, each step's artifacts, and full LLM
transcripts.
